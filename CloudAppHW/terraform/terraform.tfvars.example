# =============================================================================
# Terraform Variables Example
# 範例設定檔 - 請複製為 terraform.tfvars 並填入實際值
# =============================================================================
#
# 使用方式：
# 1. 複製此檔案: cp terraform.tfvars.example terraform.tfvars
# 2. 編輯 terraform.tfvars 並填入你的實際設定
# 3. 確保 credentials.json 存在於同一目錄
#
# 注意：terraform.tfvars 包含敏感資訊，已加入 .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# 必要設定 (Required)
# -----------------------------------------------------------------------------

# 你的 GCP Project ID
# 可在 Google Cloud Console 首頁找到，或執行: gcloud config get-value project
project_id = "your-project-id-here"

# Service Account JSON 金鑰檔案路徑
# 從 Google Cloud Console → IAM → Service Accounts 下載
credentials_file = "credentials.json"

# -----------------------------------------------------------------------------
# 區域設定 (Region)
# -----------------------------------------------------------------------------

# 選擇最接近你的區域以獲得最佳效能
# 歐洲: europe-west1 (比利時), europe-west3 (法蘭克福)
# 美國: us-central1 (愛荷華), us-east1 (南卡羅來納)
# 亞洲: asia-east1 (台灣), asia-northeast1 (東京)
region = "europe-west1"

# -----------------------------------------------------------------------------
# Cluster 設定
# -----------------------------------------------------------------------------

cluster_name  = "cloud-app-cluster"
environment   = "dev"  # dev, staging, prod
k8s_namespace = "default"

# -----------------------------------------------------------------------------
# 節點設定 (調整以控制成本)
# -----------------------------------------------------------------------------

# 機器類型 (影響成本和效能)
# e2-small: 最便宜，適合測試
# e2-medium: 開發推薦
# e2-standard-2: 生產環境
machine_type = "e2-small"

# 節點數量
node_count     = 2  # 初始節點數
max_node_count = 4  # 自動擴縮最大值

# 使用可搶佔節點可節省 60-80% 成本
# 注意：可搶佔節點最多運行 24 小時
use_preemptible = true

# -----------------------------------------------------------------------------
# 應用程式機密 (Secrets)
# 這些值會在部署 K8s 資源時使用
# -----------------------------------------------------------------------------

# Gemini API Key (留空則跳過設定)
gemini_api_key = ""

# GCP Service Account JSON (留空則跳過設定)
gcp_service_account_json = ""
